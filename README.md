# Recipe-Genie

A simple web application to generate or suggest recipes (frontend) — fast, lightweight, and easy to get running.

## 🎯 What is Recipe-Genie

Recipe-Genie is a React + Vite based frontend app (with minimal HTML/CSS/JS) which serves as the user-interface for browsing or generating recipes.  
It is designed to be fast and easy to deploy.

You can check out the live version here:  
[recipe-genie-sand.vercel.app](https://recipe-genie-sand.vercel.app)

## 📦 Tech Stack

- **JavaScript** — core logic
- **React** — UI components and rendering
- **Vite** — build tool / dev server
- **HTML & CSS** — markup and styling
- Optionally: any backend or API (if you integrate one later)

## 🚀 Getting Started (Local Development)

To run the project locally:

```bash
git clone https://github.com/Abdul-Rafay-Munir/recipe-genie.git
cd recipe-genie
npm install
npm run dev
```
